[
  {
    "name": "setup-token",
    "description": "Configure the token for your server (admin only)",
    "default_member_permissions": "8"
  },
  {
    "name": "setup-channels",
    "description": "Configure the channels for your server (admin only)",
    "default_member_permissions": "8"
  },
  {
    "name": "mint",
    "description": "Mint tokens for users (admin only)",
    "default_member_permissions": "8",
    "options": [
      {
        "name": "users",
        "description": "User(s) to mint tokens for (mention one or more users)",
        "required": true,
        "type": 3
      },
      {
        "name": "amount",
        "description": "Number of tokens to mint (per user)",
        "type": 10,
        "required": true,
        "min_value": 0.01
      },
      {
        "name": "message",
        "description": "Optional message for the notification (e.g., reason for minting)",
        "type": 3,
        "required": false
      }
    ]
  },
  {
    "name": "edit-rewards",
    "description": "Configure token rewards for a role (admin only)",
    "default_member_permissions": "8",
    "options": [
      {
        "name": "role",
        "description": "The role to configure rewards for",
        "type": 8,
        "required": true
      },
      {
        "name": "amount",
        "description": "Number of tokens to mint",
        "type": 10,
        "required": true,
        "min_value": 1
      },
      {
        "name": "frequency",
        "description": "How often to mint tokens",
        "type": 3,
        "required": true,
        "choices": [
          { "name": "Daily", "value": "daily" },
          { "name": "Weekly", "value": "weekly" },
          { "name": "Monthly", "value": "monthly" }
        ]
      }
    ]
  },
  {
    "name": "edit-costs",
    "description": "Configure token costs for a role (admin only)",
    "default_member_permissions": "8",
    "options": [
      {
        "name": "role",
        "description": "The role to configure costs for",
        "type": 8,
        "required": true
      },
      {
        "name": "amount",
        "description": "Number of tokens to burn",
        "type": 10,
        "required": true,
        "min_value": 1
      },
      {
        "name": "frequency",
        "description": "How often to burn tokens",
        "type": 3,
        "required": true,
        "choices": [
          { "name": "Daily", "value": "daily" },
          { "name": "Weekly", "value": "weekly" },
          { "name": "Monthly", "value": "monthly" }
        ]
      }
    ]
  },
  {
    "name": "book",
    "description": "Book a meeting room",
    "options": [
      {
        "name": "room",
        "description": "The meeting room to book",
        "type": 3,
        "required": true,
        "autocomplete": true
      },
      {
        "name": "when",
        "description": "When to book (e.g., 'tomorrow 2pm', 'next Monday 10am', '14:00')",
        "type": 3,
        "required": true
      },
      {
        "name": "duration",
        "description": "Duration (e.g., '1h', '30m', '2h') - default: 1h",
        "type": 3,
        "required": false
      },
      {
        "name": "name",
        "description": "Event name (optional, e.g., 'Team Sync', 'Client Meeting')",
        "type": 3,
        "required": false
      }
    ]
  },
  {
    "name": "cancel",
    "description": "Cancel one of your upcoming room bookings"
  }
]
